### app_ui.R ###

All the main structure -----------

Generic : use cards from here to wrap module calls ? or use card inside modules ? Think well before doing either
	  add logo in navbar 
	  add bsicons and replace all icons from this

bs4Dash:: calls -> bslib calls 

	All the fluidRows + columns() calls --> bslib::layout_column()/bslib::layout_column_wrap() ? 
		See https://rstudio.github.io/bslib/articles/column-layout/index.html
	
	dashboardPage()
	change theme (no more freshTheme)
	dashboardHeader() -> shift all menuItems into nav_items from sidebar inside the new bslib navbar_page()
	dropdownMenu() in header
	tags$footer() -> à modifier ?

	dashboardBody()
	all tabItems -> adapted for navbar_page() calls
	add card() for the leaflet map (modularize it later)

	tabScribe -> see if still needed. maybe not. slows down.

### app_server.R ###

No change needed if the map is not modularized

Shinybrowser usage can be kept for a while, but likely it's not needed anymore since bs5 is more dynamic

### mod_inputs.R ###

+ MOVE UNIT CONVERTER MODULE HERE
Except that, other widgets will still be set in the bslib sidebar, not much should change I think
(no fluidRows+column() calls here)



### mod_[datatype].R (mod_regener_cons_charts.R, mod_elec_charts.R, etc.) ###

All bs4Dash:: calls shift to shiny or bslib (e.g. tabsetPanel from shiny instead of bs4Dash)
Keep shinywidgets
Column() calls should be replaced by layout_column(_wrap()?)

### mod_download_rmd.R ###

No important change needed here I think

### mod_download_data.R

fluidRow go out in favour of div + flex ? or maybe not even needed. Not clear.

### mod_unit_converter.R ###

See files H: disk ? No change needed except tweaks about class, etc.

### utils_helpers.R ###

No change needed likely

### fct_helpers.R ###

Massive changes in all the plotting functions 

inputVals$web_width/height can be kept for safety but shouldn't be used and should be gradually removed
ggplotly's width+height args should be dealt with the example in H: disk. 
return_dynamic_size() should not be needed anymore -> use layout_columns(_wrap()?) + breakpoints() instead


	